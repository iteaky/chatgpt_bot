name: Vinted wakeboard monitor

on:
  schedule:
    # Every 5 minutes, offset from the busiest exact-minute boundaries.
    - cron: "2-59/5 * * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/vinted-monitor.yml"
      - "monitor.py"
      - "send_telegram.py"
      - "requirements.txt"

permissions:
  contents: write

concurrency:
  group: vinted-wakeboard-monitor
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 6

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Check Vinted
        run: python monitor.py

      - name: Send Telegram cards
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python send_telegram.py

      - name: Save seen listing IDs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add seen.json
          git diff --cached --quiet || git commit -m "Update seen Vinted listings"
          git pull --rebase origin main
          git push
